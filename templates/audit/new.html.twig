{% extends 'base.html.twig' %}

{% block title %}Nouvel audit - {{ parent() }}{% endblock %}

{% block content %}
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h3 class="mb-0"><i class="bi bi-plus-circle"></i> Lancer un nouvel audit RGAA</h3>
            </div>
            <div class="card-body">
                <p class="text-muted">
                    Entrez l'URL du site web que vous souhaitez auditer pour l'accessibilité RGAA.
                    L'audit analysera automatiquement la page avec Playwright et Pa11y, puis générera
                    des recommandations détaillées avec l'IA.
                </p>

                <form method="post" action="{{ path('app_audit_new') }}">
                    <div class="mb-3">
                        <label for="url" class="form-label">URL du site à auditer</label>
                        <input type="url"
                               class="form-control form-control-lg"
                               id="url"
                               name="url"
                               placeholder="https://example.com"
                               required
                               autofocus>
                        <div class="form-text">
                            Exemple : https://www.example.com ou https://example.com/page
                        </div>
                    </div>

                    <div class="alert alert-info">
                        <h6><i class="bi bi-info-circle"></i> Ce qui sera analysé :</h6>
                        <ul class="mb-0">
                            <li><strong>Playwright (Axe-core + HTML_CodeSniffer)</strong> : Navigation clavier, gestion du focus, éléments interactifs, structure HTML/CSS, contraste, sémantique, ARIA</li>
                            <li><strong>Gemini AI</strong> : Contextualisation et recommandations détaillées</li>
                        </ul>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary btn-lg" id="submit-btn">
                            <i class="bi bi-rocket-takeoff"></i> Lancer l'audit
                        </button>
                        <a href="{{ path('app_dashboard') }}" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left"></i> Retour au dashboard
                        </a>
                    </div>
                </form>

                <!-- Progression de l'audit -->
                <div id="audit-progress" class="mt-4" style="display: none;">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <span class="spinner-border spinner-border-sm me-2" role="status"></span>
                                Audit en cours...
                            </h5>
                            <p class="text-muted mb-3">Cela peut prendre quelques minutes. Merci de patienter.</p>

                            <div class="progress mb-3" style="height: 25px;">
                                <div id="progress-bar" class="progress-bar progress-bar-striped progress-bar-animated"
                                     role="progressbar" style="width: 0%">0%</div>
                            </div>

                            <div class="list-group">
                                <div id="step-1" class="list-group-item">
                                    <div class="d-flex align-items-center">
                                        <span class="spinner-border spinner-border-sm me-2 step-spinner"></span>
                                        <i class="bi bi-check-circle-fill text-success me-2 step-check" style="display: none;"></i>
                                        <strong>Étape 1/3 :</strong>&nbsp;<span>Préparation de l'audit...</span>
                                    </div>
                                </div>
                                <div id="step-2" class="list-group-item text-muted">
                                    <div class="d-flex align-items-center">
                                        <span class="spinner-border spinner-border-sm me-2 step-spinner" style="display: none;"></span>
                                        <i class="bi bi-check-circle-fill text-success me-2 step-check" style="display: none;"></i>
                                        <strong>Étape 2/3 :</strong>&nbsp;<span>Analyse Playwright avec Axe-core et HTML_CodeSniffer...</span>
                                    </div>
                                </div>
                                <div id="step-3" class="list-group-item text-muted">
                                    <div class="d-flex align-items-center">
                                        <span class="spinner-border spinner-border-sm me-2 step-spinner" style="display: none;"></span>
                                        <i class="bi bi-check-circle-fill text-success me-2 step-check" style="display: none;"></i>
                                        <strong>Étape 3/3 :</strong>&nbsp;<span>Analyse IA Gemini (recommandations)...</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="card mt-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-question-circle"></i> À propos de l'audit RGAA</h5>
            </div>
            <div class="card-body">
                <p>
                    Le <strong>Référentiel Général d'Amélioration de l'Accessibilité (RGAA)</strong>
                    est le référentiel français pour l'accessibilité numérique, basé sur les WCAG 2.1.
                </p>
                <p>
                    Il comporte <strong>106 critères</strong> répartis en 13 thématiques pour garantir
                    l'accessibilité des sites web aux personnes en situation de handicap.
                </p>
                <h6>Niveaux de criticité :</h6>
                <ul>
                    <li><span class="badge severity-critical">Critique</span> : Bloque l'accès au contenu</li>
                    <li><span class="badge severity-major">Majeur</span> : Impact significatif sur l'expérience</li>
                    <li><span class="badge severity-minor">Mineur</span> : Amélioration recommandée</li>
                </ul>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
<script>
document.addEventListener('DOMContentLoaded', function() {
    const form = document.querySelector('form');
    const submitBtn = document.getElementById('submit-btn');
    const progressDiv = document.getElementById('audit-progress');

    form.addEventListener('submit', function(e) {
        // Afficher la progression
        submitBtn.disabled = true;
        progressDiv.style.display = 'block';

        // Simuler la progression des étapes
        let currentStep = 1;
        const totalSteps = 4;

        // Étape 1 : Immédiatement
        updateStep(1, 25);

        // Étape 2 : Après 3 secondes (Playwright avec Axe-core et HTML_CodeSniffer)
        setTimeout(() => {
            completeStep(1);
            updateStep(2, 60);
        }, 3000);

        // Étape 3 : Après 8 secondes (Gemini)
        setTimeout(() => {
            completeStep(2);
            updateStep(3, 90);
        }, 8000);
    });

    function updateStep(stepNum, progress) {
        const step = document.getElementById('step-' + stepNum);
        step.classList.remove('text-muted');
        step.classList.add('active');
        step.querySelector('.step-spinner').style.display = 'inline-block';

        const progressBar = document.getElementById('progress-bar');
        progressBar.style.width = progress + '%';
        progressBar.textContent = progress + '%';
    }

    function completeStep(stepNum) {
        const step = document.getElementById('step-' + stepNum);
        step.querySelector('.step-spinner').style.display = 'none';
        step.querySelector('.step-check').style.display = 'inline-block';
    }
});
</script>
{% endblock %}
